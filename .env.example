# ==============================================================================
# AidLink Root Environment Variables (for Docker Compose & Local Development)
# ==============================================================================
# Copy this file to .env before starting services:
# cp .env.example .env

# PostgreSQL Docker Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aidlink_db
POSTGRES_PORT=5432
